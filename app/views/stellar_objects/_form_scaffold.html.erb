<%= form_with(model: stellar_object, scope: :stellar_object, url: stellar_object_path(stellar_object), class: "contents") do |form| %>
  <div class="<%= local_assigns.fetch(:wrapper_class, 'space-y-2') %>">
    <%= render 'application/form_errors', errors: stellar_object.errors.where(:base) %>

    <%= render 'application/name_generator_field', form: form %>

    <%= yield form %>

    <% if local_assigns.fetch(:show_known_field, true) %>
      <%= render 'stellar_objects/known_field', form: form, stellar_object: stellar_object %>
    <% end %>

    <div class="<%= local_assigns[:notes_wrapper_class] %>">
      <div class="edit-label">Observations<%= required_icon(stellar_object, :notes) if local_assigns.fetch(:show_notes_required_icon, false) %></div>
      <%= form.text_area :notes, rows: 6, class: "edit-base mt-2 w-full resize-y" %>
      <%= render 'application/field_error', form: form, attribute: :notes if local_assigns.fetch(:show_notes_field_error, false) %>
    </div>

    <div class="pt-2 flex flex-col sm:flex-row gap-2">
      <%= form.submit 'Update core', class: 'w-full sm:w-auto btn btn-primary' %>
      <%= link_to 'Cancel', stellar_object_path(stellar_object), class: 'w-full sm:w-auto btn' %>
    </div>
  </div>
<% end %>
