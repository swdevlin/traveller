<%= render 'stellar_objects/form_scaffold', stellar_object: stellar_object, show_known_field: false, notes_wrapper_class: 'edit-row-notes' do |form| %>
  <%= form.fields_for :data, (stellar_object.data || {}) do |df| %>
    <%= render 'application/edit_field', form: form, attribute: :comet_type do %>
      <%= df.select :comet_type,
                    options_for_select(
                      Comet::DESCRIPTIONS.map { |value, label| [label, value] },
                      stellar_object.comet_type
                    ),
                    { include_blank: 'Select a type' },
                    class: 'edit-base w-full' %>
    <% end %>
  <% end %>
<% end %>
