<% content_for :title, "Sectors" %>
<% content_for :page_kicker, "IISS Sector Survey Registry" %>
<% content_for :page_title, "Sectors" %>

<%= render 'application/page' do %>
  <turbo-frame id="sectors">
    <%= render "list", sectors: @sectors, pagy: @pagy %>
  </turbo-frame>
  <div class="iiss-form">
    FORM 0420A-ISSR
  </div>
<% end %>
