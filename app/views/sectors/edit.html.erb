<% content_for :title, "Editing #{@sector.name}" %>

<div class="w-full">
  <div class='mt-4'>
    <div class='text-xs uppercase tracking-[0.22em] text-slate-400'>
      Sector Data Core
    </div>
    <div class='mt-1 text-2xl font-header text-deepnight-orange caps-small'>
      <%= @sector.name %>
    </div>
  </div>

  <%= render "form", sector: @sector %>
</div>
