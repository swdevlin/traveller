<% content_for :title, "Editing #{@allegiance.name}" %>

<div class="w-full">
  <div class='mt-4'>
    <div class='text-xs uppercase tracking-[0.22em] text-slate-400'>
      Allegiance edit console
    </div>
    <div class='mt-1 text-2xl font-header text-deepnight-orange'>
      <%= @allegiance.name %>
    </div>
  </div>

  <%= render "form", allegiance: @allegiance %>
</div>
