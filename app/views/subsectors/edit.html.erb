<% content_for :title, "Editing #{@subsector.name}, #{@subsector.sector.name}" %>

<div class="w-full">
  <h1 class="font-bold text-4xl">Editing subsector</h1>

  <%= render "form", subsector: @subsector %>

  <%= link_to "Back to #{@subsector.name}", @subsector, class: "w-full sm:w-auto btn" %>
</div>
