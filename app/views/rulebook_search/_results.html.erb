<%# locals: groups, query %>
<% if query.blank? %>
  <%= render 'empty_state' %>
<% elsif groups.empty? %>
  <%= render 'no_results', query: query %>
<% else %>
  <div class='space-y-4'>
    <% groups.each do |group| %>
      <%= render 'group', group: group, query: query %>
    <% end %>
  </div>
<% end %>