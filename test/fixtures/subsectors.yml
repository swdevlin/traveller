# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% (1..4).each do |y| %>
<% (1..4).each do |x| %>
subsector_<%= x %>_<%= y %>:
  name: "Subsector <%= ('A'..'P').to_a[(y-1)*4 + (x-1)] %>"
  sector: one
  x: <%= x %>
  y: <%= y %>

subsector_2_<%= x %>_<%= y %>:
  name: "Subsector <%= ('A'..'P').to_a[(y-1)*4 + (x-1)] %>"
  sector: two
  x: <%= x %>
  y: <%= y %>
<% end %>
<% end %>