# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<%
  require_relative '../../db/seed_data/trade_codes'
  TRADE_CODES.each do |row|
%>
<%= row[:code].downcase %>:
  code: <%= row[:code] %>
  definition: <%= row[:definition].inspect %>

<% end %>